/**
 * This config is used only by merge.js.
 */

const config = {
  // contributor's name from git
  gitAuthor: "Your GitName",

  // this is how you name "All repositories I'm contributing to"
  repo: "All",

  // you need not to change the below unless you run tests
  directory: "./output/"
  
};

export default config;